#include "main.h"
#include <stdio.h>

/**
 * _atoi - String to int
 *
 * @s: input;
 *
 * Return: int.
 */
int _atoi(char *s)
{
	int i = 0;
	int x;
	int c;

	while (s[i] != '\0')
	{
		x = s[i];
		if (x < 0)
		{	
		}
	}
	
}
